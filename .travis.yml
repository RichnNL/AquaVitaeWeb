language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules

before_install:
  - npm i -g npm@6.12.0
install:
  - npm install grpc@1.20.2
  - npm install
script:
  - npm run test:coverage
  - npm run build
 
branches:
  only:
    - master
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token_rich
  local_dir: build
  on:
    branch: master